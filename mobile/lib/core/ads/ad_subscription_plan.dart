import 'package:rico_investidor/models/subscription_plan.dart';

/// Until RevenueCat is integrated, every user is treated as free for ad display.
const SubscriptionPlan kAdsSubscriptionPlan = SubscriptionPlan.free;
